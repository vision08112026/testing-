# Railway configuration
# Use nixpacks instead of Dockerfile to properly handle environment variables

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
